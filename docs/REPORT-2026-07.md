# Отчёт: производительность и юзабилити конфигуратора + правки лендинга (июль 2026)

Ветка: `claude/perf-usability-fixes` (от `claude/3d-boiler-configurator-version2`), 9 коммитов.
Тестовый деплой: **https://kotelgavno.ru/komplektacii3/** (прод `/komplektacii2/` не тронут).

## Критерии приёмки (раздел 8 спеки)

| Критерий | Статус |
|---|---|
| `npm run build` проходит, tsc без ошибок | ✅ |
| `dist/index.html` ≤ 300 КБ | ✅ **2,2 КБ** (было 14 МБ) |
| GLB — отдельные кэшируемые файлы | ✅ `dist/models/*-[hash].glb`, Cache-Control immutable 1 год (`.htaccess`), MIME `model/gltf-binary` |
| Суммарный трафик стартовой сцены ≤ 3 МБ | ✅ **~1,66 МБ**: JS 1152 КБ + CSS 16 КБ + модели standard-сцены 473 КБ (после gzip ≈ 0,8 МБ) |
| Экран загрузки с реальным прогрессом | ✅ useProgress: «Загружаем корпус котла… 64%», фирменный фон |
| Переключение комплектаций без пустот и красных сфер | ✅ полупрозрачный плейсхолдер-бокс на месте детали (и при ошибке загрузки) |
| В прод-сборке нет debug-сфер, AdminPanel, error-оверлея | ✅ grep по dist чистый; AdminPanel только в dev-бандле; error-оверлей активен только при `?debug` |
| Масштабирование не заблокировано | ✅ `maximum-scale`/`user-scalable=no` убраны |
| Минимальный текст 14 px | ✅ все панельные размеры <14px подняты до 14px |
| Эмодзи заменены на SVG | ✅ `PillarIcon` — 4 line-иконки одним цветом |
| Клик по фону не дёргает камеру | ✅ одиночный клик — снять выделение; возврат — кнопка или двойной клик |
| Кнопка «К общей схеме» + хлебная крошка | ✅ видна при сдвинутой камере/открытой карточке; «Обзор котла → Датчик уровня» |
| 4 кликабельных датчика уровня в Комфорт+ | ✅ 2 аварийных + 2 рабочих на барабане, тексты simple→benefit→description |
| Режим «Безопасность» со сравнением | ✅ тумблер, янтарная подсветка, плашка «Обычно: 1 датчик → У нас: 2+2», переход в Комфорт+ |
| CTA открывает лендинг с параметрами | ✅ `…komplcldcld2/?config=…&addons=…#contact`, `VITE_LANDING_URL` в env, target=_top |
| Старт из `?config=comfort_plus` | ✅ было реализовано ранее (readInitialState), проверено |

## Размеры моделей до/после (`npm run models:optimize`, meshopt)

| Модель | До | После | Сжатие |
|---|---|---|---|
| bcv_7250.glb | 2175 КБ | 143 КБ | 15,2× |
| bcv_925.glb | 2811 КБ | 159 КБ | 17,7× |
| boiler.glb | 1590 КБ | 169 КБ | 9,4× |
| cp_930.glb | 583 КБ | 30 КБ | 19,6× |
| deaerator.glb | 1267 КБ | 163 КБ | 7,8× |
| dn32h50.glb | 1229 КБ | 92 КБ | 13,4× |
| elbow_pc_f20.glb | 154 КБ | 23 КБ | 6,8× |
| **Итого** | **9,6 МБ** (×2 из-за дублей) | **778 КБ** | **12,3×** |

Оригиналы — в `models-src/` (вкл. неиспользуемый rs_410.glb), дубликаты `public/models/` удалены.

## Изменения по файлам (кратко)

- `vite.config.ts` — многофайловая сборка, GLB → `models/[name]-[hash]`; `vite.config.singlefile.ts` + `build:singlefile` — fallback.
- `scripts/optimize-models.mjs`, `package.json` (`models:optimize`), `@gltf-transform/cli` в devDeps.
- `BoilerModel.tsx`, `PlaceholderModels.tsx` — извлечение геометрии с матрицами узлов (обязательно для KHR_mesh_quantization); `Center precise={false}` (иначе fat-line квад Edges с декомпрессионной матрицей раздувает bbox — модель съезжала на 0,87 юнита; найдено сравнением с эталоном и подтверждено симуляцией three.js в Node).
- `modelAssets.ts` (новый), `LoadingOverlay.tsx` (новый), `index.tsx` (ModelPrefetcher, lazy AdminPanel), `Scene.tsx` (PartPlaceholder, стадийность, камера), `useConfigurator.ts` (boilerReady, cameraMoved, showHotspots, safetyMode, фильтр датчика в Комфорт+).
- `PillarIcon.tsx` (новый), `IntroOverlay.tsx`, `ConfigPanel.tsx` (SVG-иконки, тумблеры «Метки»/«Безопасность», CTA на лендинг), `ResetViewButton.tsx` (переписан), `AttachablePart.tsx` (Hotspot, safety-подсветка, reduced-motion), `SafetyBanner.tsx` (новый), `App.css`, `index.html`, `utils/device.ts` (prefersReducedMotion).
- `configurations.ts` — 4 датчика уровня в Комфорт+, флаг safety, тексты «2+2».
- `.github/workflows/deploy.yml` — деплой всего `dist/`; `public/.htaccess` — кэш; `docs/EMBED.md`, `.env.example`.

## Лендинг komplcldcld2 (P2)

Исходники не найдены (репо `komplektacii` — другая серия лендингов) → правки внесены в собранный экспорт на Beget, бэкап: `/komplcldcld2_backup_20260706/`. Подробности и инструкция по переносу в исходники: `docs/landing-fixes.md`.

- ✅ og:url исправлен (был `/komplcldcld1/`) — в HTML и RSC/txt-payload'ах.
- ✅ Яндекс.Карта (Энгельс, Демократическая 1) вместо TODO-плейсхолдера — через `landing-enhance.js`.
- ✅ Кнопки «Посмотреть в 3D» в 3 карточках комплектаций + в секции подбора (ссылка собирается по текущему выбору).
- ✅ Дубль «Подходит, если» в секции подбора скрыт, вместо него ссылка на сравнение.
- ✅ Форма читает `?config=&addons=`: плашка «Вы выбрали: Комфорт Плюс + деаэратор» + автозаполнение поля «Что нужно».
- ✅ Mixed content (логотипы) — уже был исправлен ранее, изменений не потребовалось.

## Что осталось на следующий этап

- Полный визуальный редизайн по макетам (вне рамок этой задачи по спеке).
- Перенос правок лендинга из `landing-enhance.js` в настоящие исходники Next.js, когда найдутся (см. `docs/landing-fixes.md`); в статическом HTML без JS плейсхолдер карты остаётся виден.
- Перевод прода: заменить `/komplektacii2/` новой сборкой (обновить секрет `FTP_SERVER_DIR` воркфлоу или залить вручную) и обновить iframe-встраивания.
- Опционально: code-splitting three.js-бандла (1,15 МБ, 332 КБ gzip) и KTX2 — сейчас необходимости нет.
- `VITE_LEAD_ENDPOINT` по-прежнему не задан (лиды идут через postMessage).

# Куда отправлять заявки с формы КП (POST JSON). Пусто — только postMessage в родительское окно.
VITE_LEAD_ENDPOINT=

# Лендинг с формой КП, куда ведёт кнопка «Получить КП по этой сборке».
# Домен временный: сейчас kotelgavno.ru (dev), после переезда — kotelpremium.ru.
VITE_LANDING_URL=https://kotelgavno.ru/komplcldcld2/

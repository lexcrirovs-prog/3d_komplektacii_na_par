# Проверка версии 2026.09.08.2

Дата: **8 сентября 2026 года**. Автор: **Codex / GPT-6 Astra**.

Результат: **PASSED_LIVE_LOCAL_MCP + PASSED_CODEX_NATIVE_MCP**.

## Подтверждено

- 16 автоматических проверок прошли. Проверены выбор открытого документа, неоднозначные имена, ограничения страниц, недопустимые идентификаторы, оба способа возврата габаритов COM, защита от подключения к другой установке AutoCAD, занятость приложения и сохранность остальных настроек Codex. Дополнительно проверены метаданные постоянных атрибутов и больших полилиний, а также обновление точного прежнего блока настроек и отказ при изменённом блоке.
- Для итоговой версии 2026.09.08.2 два отдельных живых MCP-сеанса прошли инициализацию, получение каталога из шести инструментов и чтение тестового чертежа. Первый использовал исходный сервер, второй — установленную копию. Протокол: `2025-11-25`. Оба запуска прошли с выключенным Tech Preview и закрытым Autodesk Assistant.
- Ранее на версии 2026.09.08.1 дополнительно проверено чтение неактивного чертежа. MCP прочитал явно выбранный документ, сохранив активную вкладку и состояние открытых документов; код выбора документа в итоговой версии сохранён.
- Прочитаны слои, страницы объектов, линия, окружность, трёхмерное тело, полилиния, текст и блок с атрибутом `TAG=MCP-001`.
- Длина линии: **1000 мм**. Радиус окружности: **250 мм**. Габариты тела: **1000 × 600 × 600 мм**. Объём: **360 000 000 мм³**. Bulge первой вершины полилинии: **0,5**.
- Недопустимое имя документа, недопустимый handle, страница более 200 объектов и отсутствующий инструмент выполнения команд отклонены.
- Состояние чертежа и SHA-256 сохранённого DWG не изменились после чтения.
- `codex mcp get autocad --json` подтвердил включённое подключение, STDIO, шесть разрешённых инструментов, ожидание запуска 30 с и ожидание операции 60 с.
- Проверены хеши шести установленных файлов. Все байты вне управляемой секции AutoCAD сохранены при обновлении и семантически совпадают с резервной копией. Установленная версия 2026.09.08.1 также сохранена в отдельном каталоге.
- Blender после установки AutoCAD-подключения ответил своим MCP-инструментом `blender_status`: `connected=true`, Blender `3.3.3`, bridge `2026.09.08.1`.
- После перезапуска Codex 8 сентября 2026 года все шесть инструментов AutoCAD доступны непосредственно в задаче. Через них повторно прочитаны список документов, единицы, слои, две страницы объектов и свойства всех шести тестовых объектов. Размеры, текст и атрибут совпали с ожидаемыми значениями. Состояние документа и SHA-256 DWG после чтения сохранились.
- AutoCAD был повторно запущен пользователем. Подключение к новому сеансу подтвердилось: `connected=true`, `ready=true`. Тестовый DWG открыт отдельным локальным вызовом ActiveX в режиме только чтения; открытие файла не является функцией MCP-сервера. Уведомление Drawing Recovery и вспомогательное окно Autodesk были закрыты перед чтением.

## Локальные свидетельства

Файлы расположены относительно `%USERPROFILE%\.codex\tools\autocad-connection` и не включены в Git:

- `runtime/source-smoke-v2.json` — живой MCP-сеанс итоговой версии из исходного кода;
- `runtime/installed-smoke-v2.json` — проверка установленной версии 2026.09.08.2;
- `runtime/codex-native-acceptance-20260908.json` — приёмка непосредственно инструментами текущей задачи Codex после перезапуска;
- `runtime/source-smoke-evidence.json` и `runtime/installed-smoke-evidence.json` — более ранние проверки 2026.09.08.1;
- `runtime/outputs/autocad-mcp-check-20260908T091301-ce4937fd.dwg` — отдельный синтетический чертёж;
- `runtime/outputs/autocad-mcp-check-20260908T091301-ce4937fd.json` — его идентификаторы и ожидаемые размеры;
- `installation.json` — версия, хеши, расположение и итог проверки;
- `backups/config-before-autocad-*.toml` — резервная копия настроек. Не публиковать её содержимое.

## Практические пределы результата

Каталог текущей задачи Codex и фактическое чтение через него проверены. Дополнительный перезапуск для установленной версии не требуется. Blender также повторно ответил непосредственно в задаче после перезапуска.

Реконструкция реальной котельной, чтение специальных связей Plant 3D, трассировка труб, проверка коллизий, изменение рабочего проекта, GLB-сборка для сайта и подготовка STL здесь не выполнялись. Подключение AutoCAD этой версии предназначено для чтения стандартных свойств DWG.

Повторное подключение проверено отдельными MCP-процессами и после перезапуска AutoCAD пользователем. Работа с другим выпуском AutoCAD, другой COM-регистрацией, одновременно запущенными экземплярами одной версии и очень крупным реальным проектом пока не подтверждена.

Первый живой тест выявил различие pywin32: `GetBoundingBox` вернул координаты как результат метода, а не через переданные VARIANT. Обработка обоих форматов добавлена и проверена тестами, затем повторена живая проверка.

Отдельное ревью обнаружило неполное описание атрибутов и пропажу метаданных OCS при усечении очень большой полилинии. В версии 2026.09.08.2 явно обозначено, что читаются только редактируемые атрибуты, и сохранена выдача OCS/первых 200 bulge для ограниченного массива координат. Оба сценария покрыты регрессионными тестами.
